function hide () {
    var button = document.getElementById('hide').offsetWidth()
    var nav = document.getElementsByTagName('nav')
    var body = document.body()

    if (nav.width() > 50) {
        body.style.grid-template-columns()
    }
}

